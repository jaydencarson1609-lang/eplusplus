{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "E++: Run current file",
      "type": "node-terminal",
      "request": "launch",
      "command": "python3 ${workspaceFolder}/epp.py ${file}"
    }
  ]
}
